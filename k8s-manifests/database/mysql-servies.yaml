apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: three-tier
spec:
  clusterIP: None   # headless - required for StatefulSet stable network identity
  selector:
    app: mysql
  ports:
  - port: 3306
    targetPort: 3306